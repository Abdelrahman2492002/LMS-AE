const TheadCell = ({ children }) => {
  return (
    <th className="text-15 text-fontBlack truncate px-2 py-3 font-semibold md:text-base">
      {children}
    </th>
  );
};

export default TheadCell;
