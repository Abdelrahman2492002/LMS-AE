const TheadCell = ({ children }) => {
  return (
    <th className="text-15 truncate px-2 py-3 font-semibold text-[#252525] md:text-base">
      {children}
    </th>
  );
};

export default TheadCell;
